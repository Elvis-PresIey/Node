#pragma once

#ifndef __FUNCTIONS_H__
#define __FUNCTIONS_H__

#include <stdio.h>
#include "CNode.h"

void print(CNode*);    // Функция, осуществляющая печать списка.
CNode* merge(CNode*, CNode*);  // Функция слияния списков.

#endif
